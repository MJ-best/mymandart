// Update this page (the content is just a fallback if you fail to update the page)

import MandalartApp from "@/components/MandalartApp";

const Index = () => {
  return <MandalartApp />;
};

export default Index;
